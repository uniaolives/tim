package org.sasc.sentinel

object NetworkClient {
    fun sendTorPost(url: String, path: String, payload: Map<String, String>) {
        // Placeholder para envio de dados via proxy Tor (SOCKS5)
        println("NetworkClient: Posting to $url$path via Tor")
    }
}
